{% extends "WebsiteBundle::layout.html.twig" %}

{% block title %}{{ parent() }} - Index{% endblock %}

{% block body %}
  <iframe width="100%" height="350" scrolling="no" frameborder="no" src="https://w.soundcloud.com/player/?url=https%3A//api.soundcloud.com/playlists/284908076&amp;color=00cc11&amp;theme_color=242424&amp;auto_play=false&amp;hide_related=false&amp;show_comments=true&amp;show_user=true&amp;show_reposts=false">
  </iframe>
<!--
  <div class="panel panel-default">
    <div class="panel-heading">
      <h4 class="panel-title">Ajouter un media (mp3 uniquement!)</h4>
    </div>
    <div class="panel-body">
      {{ render(controller('WebsiteBundle:Media:add', { 'request': app.request })) }}
    </div>
  </div>

  {% for media in medias %}
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">
          {{ media.title }}
        </h3>
      </div>
      <div class="panel-body">
        <audio controls>
          <source src="{{ asset('audio/' ~ media.path) }}" type="audio/mpeg" >
        </audio>
      </div>
    </div>
  {% else %}
    <p class="panel panel-default">
      Il n'y a pas encore de medias à afficher
    </p>
  {% endfor %}

  -->
{% endblock %}
